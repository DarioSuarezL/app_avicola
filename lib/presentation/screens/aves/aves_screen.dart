import 'package:flutter/material.dart';

class AvesScreen extends StatelessWidget {

  static const routeName = 'aves_screen';
  const AvesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Aves Screen');
  }
}